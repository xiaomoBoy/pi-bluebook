---
title: 登录账号，让 Pi 能回答你
description: 完成首次认证，选择可用模型，完成一次无文件操作的连通测试。
prev:
  text: 安装并启动 Pi
  link: /guide/install-pi
next:
  text: 从练习目录开始
  link: /guide/ready-to-work
---

<span class="library-status">MODULE 01 · STEP 03</span>

# 登录账号，让 Pi 能回答你

Pi 提供交互界面和文件工具，真正生成回复的是你选择的模型服务。所以安装完 Pi 后，还需要连接一个你已经可以使用的服务。

## 1. 选择一种登录方式

如果你上一课后已经退出 Pi，先在终端重新进入练习目录。

```bash
cd ~/Downloads/pi-practice
pwd
pi
```

确认 `pwd` 以 `/Downloads/pi-practice` 结尾。进入 Pi 后输入：

```text
/login
```

按界面提示选择一条路线。

- 已有受支持的订阅，选择对应服务，按浏览器授权流程完成登录。
- 已有 API Key，选择对应服务，只在 Pi 本机登录界面中输入。

官方当前支持的订阅和提供商会变化，请以 [Pi Providers](https://pi.dev/docs/latest/providers) 为准。某个聊天产品账号存在，不代表它一定包含 Pi 所需的模型访问权限。

::: danger 不要公开凭据
不要把真实 API Key 贴进聊天、教程、截图或 GitHub 仓库。如果怀疑密钥已泄露，立即到服务商后台撤销旧密钥并创建新密钥。
:::

## 2. 选择当前账号可用的模型

登录返回 Pi 后，输入：

```text
/model
```

也可以按 `Ctrl+L` 打开模型选择器。先从已登录的服务中选一个实际可用的模型，然后查看底部状态栏，确认提供商和模型名已经显示。

如果希望下次启动继续使用它，在模型选择器中按 `Ctrl+S` 保存为启动默认值。不要把教程中的某个模型名称当成唯一正确答案，模型的名称、可用性和计费都会变化。

## 3. 做一次无文件操作的连通测试

回到编辑区，提交这句话：

```text
只回复“Pi 已连接”。不要读取、创建、修改文件，也不要运行命令。
```

看到真实的模型回复才算通过。只看到“登录成功”提示，还不能证明账号具备实际调用权限。

如果 Pi 准备读写文件或运行命令，按 `Esc` 中止。这次测试不需要任何工具。

::: warning 没有收到回复
- 看不到可用模型，先回到 `/login` 检查认证。
- 出现 `unauthorized`、余额或额度错误，保留原始文字，检查当前账号权限。
- 浏览器授权后没有回到 Pi，取消本次登录，确认浏览器中的账号后再重试。
:::

## 本课验收

- `/login` 完成了一种可用的认证。
- 底部状态栏显示当前模型。
- “Pi 已连接”测试得到回复，且没有工具调用。

[下一课，在正确的目录中准备工作 →](/guide/ready-to-work)
