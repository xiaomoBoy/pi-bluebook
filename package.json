{
  "name": "pi-bluebook",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "docs:dev": "vitepress dev docs",
    "docs:build": "vitepress build docs",
    "docs:check": "npm run docs:build && node scripts/check-seo.mjs",
    "docs:preview": "vitepress preview docs",
    "deploy": "npm run docs:build && wrangler deploy",
    "deploy:dry-run": "npm run docs:build && wrangler deploy --dry-run"
  },
  "devDependencies": {
    "vitepress": "1.6.4",
    "wrangler": "4.130.0"
  }
}
